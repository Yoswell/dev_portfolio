export const HashTag = ({children}: any) => {
    return (
        <div className="bg-bg2 text-fg2 text-xs border border-x border-b-2 border-black outline outline-1 outline-br3 rounded py-px px-2 code">
            {children}
        </div>
    )
}